name: file_ziaroh
description: Aplikasi Dokumentasi Ziaroh Suran PP. Darul Musthofa

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  shared_preferences: ^2.2.2
  video_player: ^2.8.3
  chewie: ^1.7.4
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  path: ^1.9.0
  permission_handler: ^11.3.0
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
